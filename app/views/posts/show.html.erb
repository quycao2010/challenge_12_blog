<div class="mt-3 ml-3 mr-3">
<h2> <%= @post.title %></h2>
<div class="card">
    <div class="card-body">
        <%= @post.content %>
    </div>
    <div class="card-footer">
          Submited <%= time_ago_in_words(@post.created_at)%>
    </div>
</div>
</div>